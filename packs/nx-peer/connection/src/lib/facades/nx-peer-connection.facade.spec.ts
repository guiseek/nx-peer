import { NxPeerConnection } from './nx-peer-connection.facade';

describe('NxPeerConnection', () => {
  it('should create an instance', () => {
    expect(new NxPeerConnection()).toBeTruthy();
  });
});
